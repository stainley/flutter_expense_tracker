import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Main', () {
    test('Counter increments smoke test', () {
      const counter = 0;

      expect(counter, 0);
    });
  });
}
